" Choose light or dark
set background=dark
let g:one_allow_italics=1
let g:airline_theme='one'
if (empty($TMUX))
	if (has("termguicolors"))
		set termguicolors
	endif
endif
colorscheme one	" GUI theme, choose one or one-dark
